version: "3.8"
services:
  piston:
    image: ghcr.io/engineer-man/piston
    restart: unless-stopped
    ports:
      - "2000:2000"
    privileged: true
    volumes:
      - piston-packages:/piston/packages

volumes:
  piston-packages:
